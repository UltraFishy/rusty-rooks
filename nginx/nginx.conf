events {}

http {
    upstream backend {
        server client:1234;  # Client service name and port
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}
